# Changelog — {{title}}

What changed in **{{title}}**'s own content: everything under
`games/{{slug}}/`. Engine changes are in the repository's root `CHANGELOG.md`.
Record each change under `## [Unreleased]` in the same commit as the change
itself; a release renames that section to `## [x.y.z] — YYYY-MM-DD`, using
the repository's version number (AGENTS.md, "Docs move with the change").

The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

## [Unreleased]

### Added

- Scaffolded from the story template.
