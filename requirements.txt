pyyaml>=6.0
pytest>=8.0
httpx>=0.27.0
flask>=3.0.0
flask-socketio>=5.3.0
python-socketio>=5.11.0

# OPTIONAL. Push-to-talk's default speech-to-text provider. Everything else in
# the game -- including the whole test suite -- runs without it; the import is
# lazy and its absence degrades to a legible message on the mic button rather
# than an ImportError at startup. Installing it also pulls CTranslate2, and the
# first transcription downloads the model named by stt.whisper.model.
faster-whisper>=1.0.0

# OPTIONAL. The MCP tool layer -- engine/mcp/skills_server.py, which is how a
# model reaches the @skill registry at all. Absent, `lmstudio.mcp.enabled` has
# no effect, the server never starts, and the game plays and tests exactly as
# before; tool calling is simply off. Pinned to the version CosySim runs
# against a live LM Studio, because that is the combination this was ported
# from rather than reasoned out of the spec.
fastmcp>=3.2.4
